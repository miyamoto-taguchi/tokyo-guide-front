import React from "react";
import { StarIcon } from "@radix-ui/react-icons";

/**
 * 星(図形内空白)アイコン
 */
const Star = () => {
  return <StarIcon className="h-8 w-8 text-gray-300" />;
};

export default Star;
